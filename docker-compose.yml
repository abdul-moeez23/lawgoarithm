services:
  db:
    image: pgvector/pgvector:pg15
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    environment:
      - POSTGRES_DB=lawyerplatform_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5435:5432"

  pgadmin:
    image: dpage/pgadmin4
    environment:
      - PGADMIN_DEFAULT_EMAIL=admin@admin.com
      - PGADMIN_DEFAULT_PASSWORD=admin
    ports:
      - "5051:80"
    depends_on:
      - db

  web:
    build: .
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
      - ./media:/app/media
    ports:
      - "8001:8000"
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=lawyer_platform.settings.docker
      - DB_ENGINE=django.db.backends.postgresql
      - DB_NAME=lawyerplatform_db
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_HOST=db
      - DB_PORT=5432
      - DEBUG=True
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL=${GEMINI_MODEL:-models/gemini-2.5-flash}
      - MATCH_CANDIDATE_LIMIT=${MATCH_CANDIDATE_LIMIT:-30}
      - MATCH_SCORE_V2_ENABLED=${MATCH_SCORE_V2_ENABLED:-1}
      - MATCH_SCORE_CALIBRATION_MODE=${MATCH_SCORE_CALIBRATION_MODE:-percentile}
      - MATCH_SCORE_MIN=${MATCH_SCORE_MIN:-50}
      - MATCH_SCORE_MAX=${MATCH_SCORE_MAX:-92}
      - MATCH_REVIEW_PRIOR=${MATCH_REVIEW_PRIOR:-0.75}
      - MATCH_REVIEW_BAYES_M=${MATCH_REVIEW_BAYES_M:-8}
      - MATCH_SUCCESS_BAYES_M=${MATCH_SUCCESS_BAYES_M:-12}
      - MATCH_REVIEW_CONFIDENCE_CAP=${MATCH_REVIEW_CONFIDENCE_CAP:-0.70}
    depends_on:
      - db

volumes:
  postgres_data:
